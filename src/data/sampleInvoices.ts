import { SampleDocument } from '../types';

// Empty array in production mode. Real invoice data will be extracted from uploaded files via Gemini AI & FastAPI backend.
export const SAMPLE_INVOICES: SampleDocument[] = [];
